//to import the library
#include <stdio.h>           // it should has '#' infront of include and no '<>' after it

// main function 
int main() {                 

    //declare an integer variable 'a' and made it 10
    int a = 10;              // it should has ';' at the end

    //it display the value of 'a' and move to the next line
    printf("a = %d\n", a);   // this line lost '\n' after '%d' and  ';' at the end 

    //it means the program is running successful
    return 0;                // the last line should has ';' at the end
    
}

